/**
 * CP5 scaffold for the `useCallFlow` extraction.
 *
 * This hook is intentionally not wired yet. CP5 will move socket connection
 * ownership here while preserving the existing ref-forwarded handler bag.
 */
export default function useSignalingSocket() {
  return {};
}
