/**
 * CP4 scaffold for the `useCallFlow` extraction.
 *
 * This hook is intentionally not wired yet. CP4 will move local preview,
 * media release, camera toggling and video toggling here after CP3 stabilizes.
 */
export default function useLocalMedia() {
  return {};
}
