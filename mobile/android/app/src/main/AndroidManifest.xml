<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- Attaching a photo from the gallery: READ_MEDIA_IMAGES replaced the
         broader READ_EXTERNAL_STORAGE for image access on Android 13+
         (API 33); the legacy permission is still declared (capped at
         maxSdkVersion 32) for older devices. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission
      android:name="android.permission.READ_EXTERNAL_STORAGE"
      android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- react-native-webrtc queries ConnectivityService during ICE gathering. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- react-native-webrtc can route call audio to Bluetooth on Android 11 and older. -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <!-- react-native-webrtc Bluetooth audio routing on Android 12+ requires a runtime grant. -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <!-- react-native-webrtc runs screen capture (getDisplayMedia) inside a
         media-projection foreground service on Android 14+. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
    <!-- react-native-callkeep registers a ConnectionService so incoming pushes can
         show a full-screen system call UI during the ringing phase. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_PHONE_CALL" />
    <uses-permission android:name="android.permission.BIND_TELECOM_CONNECTION_SERVICE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.MANAGE_OWN_CALLS" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Required to post a full-screen-intent notification (the branded
         incoming-call screen shown from CallKeep's self-managed
         `showIncomingCallUi` event, see IncomingCallNotificationModule /
         mobile/src/callKeep.js); without it a call that arrives while the
         screen is locked never wakes/unlocks to that screen. Android 14+
         restricts this to apps the user has granted special access to —
         IncomingCallNotificationModule checks `canUseFullScreenIntent()` and
         falls back to a plain (but still audible) heads-up notification when
         it is denied. -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <!-- react-native-incall-manager vibrates the device on incoming calls. -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
    <uses-feature android:name="android.hardware.audio.output" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />
    <!-- Picture-in-Picture keeps the call video visible when the app is backgrounded. -->
    <uses-feature android:name="android.software.picture_in_picture" android:required="false" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:supportsPictureInPicture="true"
        android:resizeableActivity="true"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        <!-- Deep link used by incoming-call push notifications: wetalk://call/{callId} -->
        <intent-filter>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="wetalk" android:host="call" />
        </intent-filter>
        <!-- Deep link used by chat-message notifications: wetalk://chat/{conversationId} -->
        <intent-filter>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="wetalk" android:host="chat" />
        </intent-filter>
      </activity>
      <service
        android:name=".CallForegroundService"
        android:exported="false"
        android:foregroundServiceType="camera|microphone" />
      <!-- react-native-callkeep ConnectionService: presents the OS incoming-call
           UI (Android ConnectionService / Telecom) for ring-phase pushes. The
           classes are provided by the optional react-native-callkeep native
           module; declarations are inert when the module is not installed. -->
      <service
        android:name="io.wazo.callkeep.VoiceConnectionService"
        android:label="@string/app_name"
        android:permission="android.permission.BIND_TELECOM_CONNECTION_SERVICE"
        android:foregroundServiceType="camera|microphone|phoneCall"
        android:exported="true">
        <intent-filter>
          <action android:name="android.telecom.ConnectionService" />
        </intent-filter>
      </service>
      <!-- Handles the Accept / Decline actions on WeTalk's branded incoming-call
           notification (IncomingCallNotificationModule). Not exported: only this
           app's own PendingIntents target it. -->
      <receiver
        android:name=".IncomingCallActionReceiver"
        android:exported="false" />
    </application>
</manifest>
